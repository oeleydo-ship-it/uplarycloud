<x-dashboard-layout title="Networks"><div class="page-heading"><div><p class="breadcrumb">Docker / Networks</p><h1>Networks</h1><p>Isolate services and control container communication.</p></div><button class="button button--primary"><i data-lucide="plus"></i> Create network</button></div><article class="card resource-table-card"><div class="table-toolbar"><div class="table-search"><i data-lucide="search"></i><input placeholder="Search networks…"></div><span class="table-count">{{ $networks->total() }} networks</span></div><div class="resource-table docker-table"><div class="resource-table-head"><span>Network</span><span>Server</span><span>Driver</span><span>Subnet</span><span>Containers</span><span>Access</span><span></span></div>@foreach($networks as $n)<div class="resource-table-row"><span class="resource-primary"><span class="server-icon"><i data-lucide="network"></i></span><span><strong>{{ $n->name }}</strong><small>{{ $n->scope }} scope</small></span></span><span><strong>{{ $n->server?->name ?? 'Server removed' }}</strong></span><span>{{ $n->driver }}</span><span class="mono">{{ $n->subnet ?: 'Automatic' }}</span><span>{{ $n->containers_count }}</span><span><span class="status status--{{ $n->internal?'success':'warning' }}"><i></i>{{ $n->internal?'Internal':'Public' }}</span></span><span><button class="icon-button"><i data-lucide="more-horizontal"></i></button></span></div>@endforeach</div><div class="pagination-wrap">{{ $networks->links() }}</div></article></x-dashboard-layout>
