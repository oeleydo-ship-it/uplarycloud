<x-admin-layout title="SMTP & Email"><div class="admin-heading"><div><p>SUPERADMIN / EMAIL</p><h1>SMTP & Email</h1><span>Configure transactional email delivery for the complete platform.</span></div></div><form class="card admin-form" method="post" action="{{route('admin.mail.update')}}">@csrf @method('PUT')<div class="admin-card-head"><div><h2>Mail Server</h2><p>Credentials are encrypted before storage.</p></div><span class="admin-pill">Global</span></div><div class="admin-form-grid"><label><span>Mailer</span><select name="mail_mailer"><option value="smtp" @selected(($settings['mail_mailer']??'smtp')==='smtp')>SMTP</option><option value="log" @selected(($settings['mail_mailer']??'smtp')==='log')>Log only</option></select></label><label><span>SMTP host</span><input name="smtp_host" value="{{$settings['smtp_host']??''}}" placeholder="smtp.mailgun.org"></label><label><span>SMTP port</span><input type="number" name="smtp_port" value="{{$settings['smtp_port']??587}}"></label><label><span>Encryption</span><select name="smtp_encryption">@foreach(['tls','ssl','none'] as $option)<option @selected(($settings['smtp_encryption']??'tls')===$option)>{{$option}}</option>@endforeach</select></label><label><span>Username</span><input name="smtp_username" value="{{$settings['smtp_username']??''}}"></label><label><span>Password</span><input type="password" name="smtp_password" placeholder="Leave blank to keep saved password"></label><label><span>From address</span><input type="email" name="from_address" value="{{$settings['from_address']??'noreply@uplary.com'}}" required></label><label><span>From name</span><input name="from_name" value="{{$settings['from_name']??'Uplary Cloud'}}" required></label></div><button class="button button--primary">Save Email Settings</button></form></x-admin-layout>
