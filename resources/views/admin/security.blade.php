<x-admin-layout title="Security"><div class="admin-heading"><div><p>SUPERADMIN / SECURITY</p><h1>Security Policies</h1><span>Enforce platform-wide authentication and session standards.</span></div></div><form class="card admin-form" method="post" action="{{route('admin.security.update')}}">@csrf @method('PUT')<div class="admin-card-head"><div><h2>Authentication Policy</h2><p>Global minimum standards for all customer accounts.</p></div></div><div class="admin-form-grid"><label><span>Session lifetime (minutes)</span><input type="number" name="session_lifetime" min="5" value="{{$settings['session_lifetime']??120}}" required></label><label><span>Minimum password length</span><input type="number" name="password_min_length" min="8" value="{{$settings['password_min_length']??8}}" required></label><label><span>Maximum login attempts</span><input type="number" name="login_attempts" min="1" value="{{$settings['login_attempts']??5}}" required></label></div><div class="admin-switches">@foreach([['two_factor_required','Require two-factor authentication','Force 2FA for privileged platform accounts'],['secure_cookies','Secure session cookies','Require encrypted HTTPS-only authentication cookies'],['allow_api_tokens','Allow API tokens','Permit tenants to create scoped API credentials']] as [$key,$label,$copy])<label><span><strong>{{$label}}</strong><small>{{$copy}}</small></span><input type="checkbox" name="{{$key}}" value="1" @checked(($settings[$key]??true)==1)></label>@endforeach</div><button class="button button--primary">Save Security Policies</button></form></x-admin-layout>
