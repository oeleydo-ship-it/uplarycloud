<x-admin-layout title="Users"><div class="admin-heading"><div><p>SUPERADMIN / USERS</p><h1>Users</h1><span>Manage all platform accounts and superadmin privileges.</span></div><button class="button button--primary" onclick="document.getElementById('new-user').showModal()"><i data-lucide="user-plus"></i>Add User</button></div><form class="admin-search" method="get"><i data-lucide="search"></i><input name="search" value="{{request('search')}}" placeholder="Search users by name or email"><button>Search</button></form><article class="card admin-table-card"><table><thead><tr><th>User</th><th>Access</th><th>Tenants</th><th>Email</th><th>Last active</th><th>Actions</th></tr></thead><tbody>@foreach($users as $user)<tr><td><strong>{{$user->name}}</strong><small>{{$user->email}}</small></td><td><span class="admin-pill {{$user->is_super_admin?'admin-pill--purple':''}}">{{$user->is_super_admin?'Superadmin':'Customer'}}</span></td><td>{{$user->tenants_count}}</td><td>{{$user->email_verified_at?'Verified':'Pending'}}</td><td>{{$user->last_active_at?->diffForHumans()??'Never'}}</td><td><button class="admin-edit" onclick="document.getElementById('user-{{$user->id}}').showModal()">Edit</button></td></tr><dialog id="user-{{$user->id}}" class="admin-dialog"><form method="post" action="{{route('admin.users.update',$user)}}">@csrf @method('PUT')<h2>Edit User</h2><label><span>Name</span><input name="name" value="{{$user->name}}" required></label><label><span>Email</span><input type="email" name="email" value="{{$user->email}}" required></label><label class="admin-check"><input type="checkbox" name="is_super_admin" value="1" @checked($user->is_super_admin)>Superadmin access</label><label class="admin-check"><input type="checkbox" name="email_verified" value="1" @checked($user->email_verified_at)>Email verified</label><div><button type="button" class="button button--secondary" onclick="this.closest('dialog').close()">Cancel</button><button class="button button--primary">Save User</button></div></form></dialog>@endforeach</tbody></table><div class="admin-pagination">{{$users->links()}}</div></article><dialog id="new-user" class="admin-dialog"><form method="post" action="{{route('admin.users.store')}}">@csrf<h2>Create User</h2><label><span>Name</span><input name="name" required></label><label><span>Email</span><input type="email" name="email" required></label><label><span>Temporary password</span><input type="password" name="password" required minlength="8"></label><label class="admin-check"><input type="checkbox" name="is_super_admin" value="1">Grant superadmin access</label><div><button type="button" class="button button--secondary" onclick="this.closest('dialog').close()">Cancel</button><button class="button button--primary">Create User</button></div></form></dialog></x-admin-layout>
